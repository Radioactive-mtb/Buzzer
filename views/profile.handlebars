{{! Display a user's profile information }}
<h2>Profile</h2>
<img src="/images/profile_picture.png" alt="{{username}}'s profile picture">
<p>{{bio}}</p>

{{! Display a list of the user's posts }}
<h2>Posts</h2>
<ul>
  {{#each posts as |post|}}
    <li><strong>{{post.title}}</strong> - {{post.post_content}} - {{post.user.username}} ({{ format_date post.createdAt}})</li>
  {{/each}}
</ul>